<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <padding
        android:bottom="1dp"
        android:left="2dp"
        android:right="2dp"
        android:top="1dp" />
    <solid android:color="@color/func_1" />
    <stroke
        android:width="1dp"
        android:color="@color/colorPrimary" />

    <!--这里宽高要一样 -->
    <size
        android:width="16dp"
        android:height="16dp" />
</shape>